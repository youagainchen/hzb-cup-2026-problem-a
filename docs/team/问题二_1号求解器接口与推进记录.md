# 问题二：1号求解器接口与推进记录

## 当前判断

1号不必等待2号才能开始，但正式评分与基线接入存在明确前置依赖。

- 可立即独立完成：Q1复现核对、Q2求解器输入输出约定、修复算子骨架、固定种子驱动和假评估器单元测试。
- 必须等待2号交付：绿色客户集合、Q2实例、`policy_q2.py`、统一评估器Q2扩展、违规为0的确定性基线。
- 责任边界：1号不在 `src/solver/` 内复制绿色区判断，不修改 `src/model/evaluator.py` 和2号后续维护的政策模块。

## 已完成（2026-08-18）

- Q1结果核对：38辆、98趟、总成本43,397.2628226345元，最晚返场1439.287975464分钟（约23:59:17）。
- Q1自动测试：`python -m unittest discover -s tests -v`，原有14项全部通过。
- 修复一键脚本的解释器探测：若工作区 `.venv` 文件存在但不可启动，`run_tests.cmd` 和 `run_question1.cmd` 会自动回退到可用运行时。
- 新增 `src/solver/q2_search.py`：
  - 通过 `SolutionScorer` 回调调用统一评分器；
  - 求解器只读取 `total_cost`、`policy_violation_count` 和可选可行性字段；
  - 已搭建车型重选、发车时刻修复、整趟配送序列交换；
  - 已搭建5个固定种子驱动及逐算子收敛记录；
  - 已实现整趟relocate与物理车辆数压缩，候选时序冲突由统一评分器验收；
  - 已实现带自适应破坏/修复算子权重、模拟退火接受准则和早停的通用ALNS；
  - 目标按“其他硬约束可行 → 政策违规最少 → 总成本最低”的字典序比较。
- 新增 `src/solver/q2_outputs.py`：统一导出最优路线CSV、5种子收敛日志CSV和汇总JSON。
- 新增 `src/question2.py`：正式Python运行入口；接收2号基线与统一评分回调后，自动完成5种子ALNS及结果导出。
- 新增 `src/question2_cli.py` 与 `run_question2.cmd`：通过 `模块:build_context` 动态加载2号适配器，并支持命令行配置种子和ALNS参数。
- 评分器契约已加固：总成本必须为有限非负数、违规数必须为非负整数，且评分器不得修改候选路线。
- 5种子汇总新增合规运行数、合规成本均值、总体标准差、最小值和最大值。
- 新增 `tests/test_question2_search.py`：用假评分器验证算子与端到端输出，不在生产代码中写入任何政策判定。
- 当前完整测试共22项：Q1回归14项、Q2求解器与适配器8项，全部通过。

## 等待2号的最小交接包

> 2026-08-18更新：以下交接项已全部收到，当前不再处于等待状态；本节保留作为接口审计记录。

1. Q2实例对象或构造函数，客户编号必须与距离矩阵索引一致。
2. 已由1号在 `src.q2_adapter` 提供 `build_context(data_dir: Path) -> Question2Context`，内部包装2号单一评分入口 `score = evaluate_solution(routes, ...)`。
3. 返回值至少包含：
   - `total_cost: float`
   - `policy_violation_count: int`
   - `is_feasible: bool` 或 `feasible: bool`（建议提供）
   - 若提供 `feasibility: dict[str, bool]`，1号求解器会要求全部为真。
4. 一组违规为0的确定性基线路线，包含车型、物理车号、趟次号和发车时刻。
5. 明确8:00、16:00端点是否计入限行，以及政策按到达时刻还是服务开始时刻判定。

交接完成后的正式命令：

```powershell
run_question2.cmd
```

## 交接后的接入顺序

1. 编写薄适配器，把2号统一评估器包装成 `SolutionScorer`，不改求解器政策逻辑。
2. 用2号基线跑5个固定种子，先确认所有评分字段与2号手算一致。
3. 用真实Q2实例检查整趟relocate、车辆数压缩和ALNS算子的计算时间与有效率，调整配置而不改政策逻辑。
4. 通过现有输出层生成5种子日志、最低成本合规路线和收敛记录，并与2号基线做统一口径对比。

## 交接后正式结果（2026-08-18）

- 从问题一正式98趟方案出发，识别出12次燃油车绿色节点限行违规。
- 新增政策感知排班：绿色节点顺序修复、10分钟粒度发车枚举、车型重选、物理车辆多趟复用和固定种子参数搜索。
- 锁定唯一问题一基准 `Q1_FINAL_38V_98T_43397.262823`；38辆/99趟和55辆/122趟历史版本不参与政策影响计算。
- 最低成本合规方案：98趟、37辆，总成本44,861.52元，碳排放6,823.30 kg，政策违规0次、漏单0个、超载0趟、全部24:00前返场。
- 相对问题一：总成本增加1,464.26元（3.37%），物理车减少1辆（2.63%），碳排放增加187.30 kg（2.82%）。
- 碳排放不增加的平衡方案：35辆、45,657.00元、6,602.91 kg，成本增加5.21%、碳排放下降0.50%。
- 最低成本方案车辆结构：EV-3000为10辆、29趟；FUEL-3000为27辆、69趟。五种原题车型全部进入候选集，最终只选中两种3000型车辆。
- 已补问题一证据哈希、绿色逐站审计、4个政策边界测试、15个绿色客户清单、容量余量表和成本-排放Pareto前沿。
- 正式路线、趟次汇总、参数搜索记录和影响分析均位于 `results/question2_optimized/`。
